<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Fragments.Add_Admin_User_Product_Fragment">
    <ImageView
        android:layout_width="240dp"
        android:layout_height="240dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="60dp"
        android:src="@drawable/user"
        android:id="@+id/product_image_view"/>
    <EditText
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:id="@+id/product_name_edit_text"
        android:hint="Enter Product Name"
        android:layout_below="@id/product_image_view"
        android:layout_marginTop="10dp"
        android:background="@drawable/round_shape"
        android:paddingLeft="20dp"
        android:textColorHint="@color/colorBlack"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:id="@+id/product_desciption_edit_text"
        android:hint="Enter Product Desciption"
        android:layout_below="@id/product_name_edit_text"
        android:layout_marginTop="10dp"
        android:background="@drawable/round_shape"
        android:paddingLeft="20dp"
        android:textColorHint="@color/colorBlack"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:id="@+id/product_stock_edit_text"
        android:hint="Enter Product Stock"
        android:layout_below="@id/product_desciption_edit_text"
        android:layout_marginTop="10dp"
        android:inputType="number"
        android:background="@drawable/round_shape"
        android:paddingLeft="20dp"
        android:textColorHint="@color/colorBlack"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:id="@+id/product_price_edit_text"
        android:hint="Enter Product Price"
        android:layout_below="@id/product_stock_edit_text"
        android:layout_marginTop="10dp"

        android:inputType="number"
        android:background="@drawable/round_shape"
        android:paddingLeft="20dp"
        android:textColorHint="@color/colorBlack"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/product_add_button"
        android:layout_marginTop="10dp"
        android:layout_below="@id/product_price_edit_text"
        android:text="Upload"
        android:textAllCaps="false"/>
    <ProgressBar
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:visibility="gone"
        android:id="@+id/product_progress_bar"/>




</RelativeLayout>