<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@drawable/grandient_layout"
    android:layout_height="match_parent"
    tools:context=".ProfileDetail.ProfileDetailsActivity">

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:id="@+id/feb_Action_button"
        android:layout_alignParentRight="true"
        android:backgroundTint="#C2185B"
        android:src="@drawable/update_icon" />

    <de.hdodenhof.circleimageview.CircleImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:id="@+id/user_detail_image"
        android:src="@drawable/user"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="40dp"
        />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/user_detail_image"
        android:layout_marginTop="20dp"
        android:text="First Name"
        android:id="@+id/user_detail_first_name"
        android:textSize="20dp"
        android:textColor="@color/colorwhite"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/user_detail_first_name"
        android:layout_marginTop="20dp"
        android:text="Last Name"
        android:id="@+id/user_detail_last_name"
        android:textSize="20dp"
        android:textColor="@color/colorwhite"
        />


    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/user_detail_last_name"
        android:layout_marginTop="20dp"
        android:text="Email Address"
        android:id="@+id/user_detail_email"
        android:textSize="20dp"
        android:textColor="@color/colorwhite"
        />





</RelativeLayout>